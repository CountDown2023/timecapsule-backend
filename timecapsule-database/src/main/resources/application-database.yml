spring:
  config:
    activate:
      on-profile: local, dev, test
  datasource:
    username: root
    password: root
    jpa-hibernate-ddl-auto: create
    url: jdbc:mysql://localhost:3306/timecapsule?serverTimezone=Asia/Seoul
    maximum-pool-size: 100
    connection-timeout: 3000
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hiberante.format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG


---
spring:
  config:
    activate:
      on-profile: production
  datasource:
    username: fincore22
    password: rmazhvmf123!
    jpa-hibernate-ddl-auto: create
    url: jdbc:mysql://43.200.79.62:3306/timecapsule?serverTimezone=Asia/Seoul
    maximum-pool-size: 100
    connection-timeout: 3000
    driver-class-name: com.mysql.cj.jdbc.Driver
